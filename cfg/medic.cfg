//Meeeeen

unbindall

exec default

cl_interp .0152

slot2

//Binds
bind mwheelup "slot1; r_drawviewmodel 0; -attack"
bind mwheeldown "slot2; r_drawviewmodel 0; -attack"
bind q "slot3; r_drawviewmodel 1; -attack"
bind alt "voicespam"
bind e "voicemenu 2 0; say_team there is a boi in my booty"
bind shift "uready"
bind mouse2 "+pop"

//Alias
alias uready "say_team *** fully charged ***; voicemenu 2 2"
alias +pop "+attack2; say_team *** uber/kritz used ***; voicemenu 2 3;"
alias -pop "-attack2"
alias voicespam "voicemenu 2 5; voicemenu 2 3; voicemenu 2 5"
